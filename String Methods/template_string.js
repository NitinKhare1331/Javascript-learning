let product = {name:'I_Phone',brand:'Apple',price:140000};
let str = `The name of the product is ${product.name} which belongs to ${product.brand} brand and has a price ${product.price} INR. `;
console.log(str);